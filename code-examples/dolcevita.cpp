#include <iostream>
#include <vector>
#include <map>
#include <set>
#include <algorithm>
#include <climits>
#include <cmath>
#include <string>
using namespace std;

void solve()
{
    int n;
    cin >> n;
    vector<string> input(n);
    vector<string> temp(n);

    for (int x = 0; x < n; x++) {
        cin >> input[x];
    }



}

int main()
{
    int x;
    cin >> x;

    while (x--)
    {
        solve();
    }

    return 0;
}